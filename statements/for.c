// Permet de faire une boucle (mieux que "while").
#include <stdio.h>
#include <cs50.h>

int main (void)
{
    for (int i = 0; i < 5; i++)
    // Le nombre de tour peut-pêtre séléction en modifiant le chiffre maximale (ici le "5").
    // Cette ligne peut-être interactive en demandant à l'utilisateur combien de tour il veut faire (voir "forv2").
    {
        printf("je suis un H4CK3R\n");
    }
}
