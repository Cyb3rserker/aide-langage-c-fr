// Voilà comment comparer plusieurs string.
// Penser à mettre la parenthèse au début et à la fin.
// On met en premier le string, ensuite l'élément auquel le comparer.
if ((strcmp(s1, "a") == 0
    || (strcmp(s1, "b") == 0
    || (strcmp(s1, "c") == 0
    || (strcmp(s1, "d") == 0)