// Programme introduisant les unions.

/*
    Les unions permettent, comme les structures, de regrouper 
    plusieurs types de données. La différence est que les données
    dans les unions sont toutes dans la même zone de la mémoire.
*/

#include <stdio.h>

int main (void)
{

    return 0;
}